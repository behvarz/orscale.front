export const metadata = {
  title: "About — OrScale",
  description: "Learn more about OrScale, an AI-driven automation company.",
};

export default function AboutLayout({ children }) {
  return <>{children}</>;
}
