export const metadata = {
  title: "Solutions — OrScale",
  description: "Explore OrScale’s automation and AI-powered solutions.",
};

export default function SolutionsLayout({ children }) {
  return <>{children}</>;
}
